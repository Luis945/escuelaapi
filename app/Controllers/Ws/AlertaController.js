'use strict'

class AlertaController {
  constructor ({ socket, request }) {
    this.socket = socket
    this.request = request
  }
  onIniciar({socket,request}){

  }
}

module.exports = AlertaController
